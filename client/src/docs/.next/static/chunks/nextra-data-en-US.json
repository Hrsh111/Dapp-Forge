{"/api-reference":{"title":"API Reference","data":{"":"Complete function reference for Dapp-Forge.","helper-functions#Helper Functions":"","ethern#ether(n)":"Convert to Wei.\nether(1) // \"1000000000000000000\"","tokensn#tokens(n)":"Convert to token units.\ntokens(100) // \"100000000000000000000\"","formatbalancebalance#formatBalance(balance)":"Format Wei to readable string.\nformatBalance(\"1500000000000000000\") // \"1.5000\"","interactions#Interactions":"","loadweb3dispatch#loadWeb3(dispatch)":"Initialize Web3 connection.","loadaccountweb3-dispatch#loadAccount(web3, dispatch)":"Load user's account.","loadtokenweb3-networkid-dispatch#loadToken(web3, networkId, dispatch)":"Load token contract.","loadexchangeweb3-networkid-dispatch#loadExchange(web3, networkId, dispatch)":"Load exchange contract.","makebuyorderexchange-token-web3-order-account-dispatch#makeBuyOrder(exchange, token, web3, order, account, dispatch)":"Create a buy order.","fillorderexchange-order-account-dispatch#fillOrder(exchange, order, account, dispatch)":"Fill an existing order.","selectors#Selectors":"","accountselectorstate#accountSelector(state)":"Get connected account.","filledordersselectorstate#filledOrdersSelector(state)":"Get filled orders with decorations.","orderbookselectorstate#orderBookSelector(state)":"Get buy and sell orders.","pricechartselectorstate#priceChartSelector(state)":"Get chart data."}},"/frontend/orderbook":{"title":"OrderBook Component","data":{"":"Displays all open buy and sell orders with one-click order filling functionality.","-overview#📋 Overview":"File: src/components/OrderBook.jsPurpose:\nDisplay all open buy orders (bids)\nDisplay all open sell orders (asks)\nEnable one-click order filling\nShow real-time order updates\nProvide price discovery","-visual-structure#🎨 Visual Structure":"┌──────────────────────────────────┐\n│         Order Book               │\n├──────────────────────────────────┤\n│  BUY ORDERS    │   SELL ORDERS   │\n├─────────────────┼─────────────────┤\n│ DAPP    ETH     │  DAPP    ETH    │\n├─────────────────┼─────────────────┤\n│ 10.0   0.100 ✓  │  10.0   0.110 ✓ │\n│ 15.0   0.095 ✓  │  20.0   0.115 ✓ │\n│ 20.0   0.090 ✓  │  15.0   0.120 ✓ │\n└──────────────────────────────────┘\n     (Green)            (Red)","️-component-structure#🏗️ Component Structure":"import React, { Component } from 'react'\nimport { connect } from 'react-redux'\nimport { OverlayTrigger, Tooltip } from 'react-bootstrap'\nimport Spinner from './Spinner'\nimport {\n  orderBookSelector,\n  orderBookLoadedSelector,\n  exchangeSelector,\n  accountSelector,\n  orderFillingSelector\n} from '../store/selectors'\nimport { fillOrder } from '../store/interactions'\nclass OrderBook extends Component {\n  render() {\n    return (\n      <div className=\"vertical\">\n        <div className=\"card bg-dark text-white\">\n          <div className=\"card-header\">Order Book</div>\n          <div className=\"card-body order-book\">\n            {this.props.showOrderBook ? (\n              <div>\n                {this.renderBuyOrders()}\n                <hr />\n                {this.renderSellOrders()}\n              </div>\n            ) : (\n              <Spinner type=\"table\" />\n            )}\n          </div>\n        </div>\n      </div>\n    )\n  }\n  renderBuyOrders() {\n    const { orderBook } = this.props\n    \n    return (\n      <div>\n        <table className=\"table table-dark table-sm small\">\n          <thead>\n            <tr>\n              <th>DAPP</th>\n              <th>DAPP/ETH</th>\n              <th>ETH</th>\n            </tr>\n          </thead>\n          <tbody>\n            {orderBook.buyOrders.map((order) => this.renderOrder(order, 'buy'))}\n          </tbody>\n        </table>\n      </div>\n    )\n  }\n  renderSellOrders() {\n    const { orderBook } = this.props\n    \n    return (\n      <div>\n        <table className=\"table table-dark table-sm small\">\n          <thead>\n            <tr>\n              <th>DAPP</th>\n              <th>DAPP/ETH</th>\n              <th>ETH</th>\n            </tr>\n          </thead>\n          <tbody>\n            {orderBook.sellOrders.map((order) => this.renderOrder(order, 'sell'))}\n          </tbody>\n        </table>\n      </div>\n    )\n  }\n  renderOrder(order, type) {\n    const { dispatch, exchange, account } = this.props\n    \n    return (\n      <OverlayTrigger\n        key={order.id}\n        placement='auto'\n        overlay={\n          <Tooltip id={order.id}>\n            {`Click here to ${type === 'buy' ? 'sell' : 'buy'}`}\n          </Tooltip>\n        }\n      >\n        <tr\n          className={`order-book-order order-${type}`}\n          onClick={(e) => fillOrder(dispatch, exchange, order, account)}\n        >\n          <td>{order.tokenAmount}</td>\n          <td className={`text-${type === 'buy' ? 'success' : 'danger'}`}>\n            {order.tokenPrice}\n          </td>\n          <td>{order.etherAmount}</td>\n        </tr>\n      </OverlayTrigger>\n    )\n  }\n}\nfunction mapStateToProps(state) {\n  const orderBookLoaded = orderBookLoadedSelector(state)\n  const orderFilling = orderFillingSelector(state)\n  return {\n    orderBook: orderBookSelector(state),\n    showOrderBook: orderBookLoaded && !orderFilling,\n    exchange: exchangeSelector(state),\n    account: accountSelector(state)\n  }\n}\nexport default connect(mapStateToProps)(OrderBook)","-key-features#🎯 Key Features":"","1-order-display#1. Order Display":"Buy Orders (Bids):\nrenderBuyOrders() {\n  const { orderBook } = this.props\n  \n  return (\n    <table className=\"table table-dark table-sm small\">\n      <thead>\n        <tr>\n          <th>DAPP</th>\n          <th>DAPP/ETH</th>\n          <th>ETH</th>\n        </tr>\n      </thead>\n      <tbody>\n        {orderBook.buyOrders.map((order) => (\n          <tr key={order.id} className=\"order-book-order order-buy\">\n            <td>{order.tokenAmount}</td>\n            <td className=\"text-success\">{order.tokenPrice}</td>\n            <td>{order.etherAmount}</td>\n          </tr>\n        ))}\n      </tbody>\n    </table>\n  )\n}\nSell Orders (Asks):\nrenderSellOrders() {\n  const { orderBook } = this.props\n  \n  return (\n    <table className=\"table table-dark table-sm small\">\n      <tbody>\n        {orderBook.sellOrders.map((order) => (\n          <tr key={order.id} className=\"order-book-order order-sell\">\n            <td>{order.tokenAmount}</td>\n            <td className=\"text-danger\">{order.tokenPrice}</td>\n            <td>{order.etherAmount}</td>\n          </tr>\n        ))}\n      </tbody>\n    </table>\n  )\n}","2-one-click-order-filling#2. One-Click Order Filling":"Click Handler:\nrenderOrder(order, type) {\n  return (\n    <tr\n      className=\"order-book-order\"\n      onClick={(e) => this.handleFillOrder(order)}\n    >\n      <td>{order.tokenAmount}</td>\n      <td>{order.tokenPrice}</td>\n      <td>{order.etherAmount}</td>\n    </tr>\n  )\n}\nhandleFillOrder = (order) => {\n  const { dispatch, exchange, account } = this.props\n  fillOrder(dispatch, exchange, order, account)\n}","3-tooltips#3. Tooltips":"Interactive Tooltips:\n<OverlayTrigger\n  placement='auto'\n  overlay={\n    <Tooltip id={order.id}>\n      {`Click here to ${type === 'buy' ? 'sell' : 'buy'}`}\n    </Tooltip>\n  }\n>\n  <tr onClick={...}>\n    {/* Order data */}\n  </tr>\n</OverlayTrigger>","4-real-time-updates#4. Real-time Updates":"Order Book Selector:\n// store/selectors.js\nexport const orderBookSelector = createSelector(\n  openOrders,\n  orders => {\n    // Decorate orders\n    orders = decorateOrderBookOrders(orders)\n    \n    // Sort by price\n    orders = orders.sort((a, b) => b.tokenPrice - a.tokenPrice)\n    \n    // Separate buy and sell orders\n    return {\n      buyOrders: orders.filter(o => o.orderType === 'buy'),\n      sellOrders: orders.filter(o => o.orderType === 'sell')\n    }\n  }\n)","-styling#🎨 Styling":"","color-coding#Color Coding":"/* Buy orders - green */\n.order-buy {\n  cursor: pointer;\n}\n.order-buy:hover {\n  background-color: rgba(25, 135, 84, 0.1);\n}\n.order-buy .price {\n  color: #198754;\n}\n/* Sell orders - red */\n.order-sell {\n  cursor: pointer;\n}\n.order-sell:hover {\n  background-color: rgba(220, 53, 69, 0.1);\n}\n.order-sell .price {\n  color: #dc3545;\n}\n/* Order book container */\n.order-book {\n  max-height: 400px;\n  overflow-y: auto;\n}\n.order-book table {\n  margin-bottom: 0;\n}\n.order-book-order {\n  transition: background-color 0.2s;\n}","table-styling#Table Styling":"<table className=\"table table-dark table-sm small\">\n  <thead>\n    <tr>\n      <th className=\"text-start\">DAPP</th>\n      <th className=\"text-center\">DAPP/ETH</th>\n      <th className=\"text-end\">ETH</th>\n    </tr>\n  </thead>\n  <tbody>\n    {/* Orders */}\n  </tbody>\n</table>","-data-processing#🔄 Data Processing":"","order-decoration#Order Decoration":"Add calculated fields:\nconst decorateOrderBookOrders = (orders) => {\n  return orders.map((order) => {\n    order = decorateOrder(order)\n    order = decorateOrderBookOrder(order)\n    return order\n  })\n}\nconst decorateOrder = (order) => {\n  let etherAmount\n  let tokenAmount\n  // Determine order type\n  if (order.tokenGive === ETHER_ADDRESS) {\n    etherAmount = order.amountGive\n    tokenAmount = order.amountGet\n  } else {\n    etherAmount = order.amountGet\n    tokenAmount = order.amountGive\n  }\n  // Calculate the token price to 5 decimal places\n  const precision = 100000\n  let tokenPrice = (etherAmount / tokenAmount)\n  tokenPrice = Math.round(tokenPrice * precision) / precision\n  return {\n    ...order,\n    etherAmount: ether(etherAmount),\n    tokenAmount: tokens(tokenAmount),\n    tokenPrice,\n    formattedTimestamp: moment.unix(order.timestamp).format('h:mm:ss a M/D')\n  }\n}\nconst decorateOrderBookOrder = (order) => {\n  const orderType = order.tokenGive === ETHER_ADDRESS ? 'buy' : 'sell'\n  return {\n    ...order,\n    orderType,\n    orderTypeClass: (orderType === 'buy' ? 'success' : 'danger'),\n    orderFillAction: (orderType === 'buy' ? 'sell' : 'buy')\n  }\n}","sorting-orders#Sorting Orders":"By Price:\n// Sort buy orders - highest price first\nconst sortedBuyOrders = buyOrders.sort((a, b) => \n  b.tokenPrice - a.tokenPrice\n)\n// Sort sell orders - lowest price first\nconst sortedSellOrders = sellOrders.sort((a, b) => \n  a.tokenPrice - b.tokenPrice\n)\nBy Time:\nconst sortedOrders = orders.sort((a, b) => \n  b.timestamp - a.timestamp\n)","-advanced-features#🎯 Advanced Features":"","1-order-grouping#1. Order Grouping":"Group by price level:\ngroupOrdersByPrice(orders) {\n  const grouped = {}\n  \n  orders.forEach(order => {\n    const price = order.tokenPrice.toFixed(5)\n    \n    if (!grouped[price]) {\n      grouped[price] = {\n        price: price,\n        tokenAmount: 0,\n        etherAmount: 0,\n        orders: []\n      }\n    }\n    \n    grouped[price].tokenAmount += parseFloat(order.tokenAmount)\n    grouped[price].etherAmount += parseFloat(order.etherAmount)\n    grouped[price].orders.push(order)\n  })\n  \n  return Object.values(grouped)\n}\nrenderGroupedOrders() {\n  const grouped = this.groupOrdersByPrice(this.props.orderBook.buyOrders)\n  \n  return grouped.map(level => (\n    <tr key={level.price}>\n      <td>{level.tokenAmount.toFixed(2)}</td>\n      <td>{level.price}</td>\n      <td>{level.etherAmount.toFixed(4)}</td>\n      <td className=\"text-muted\">({level.orders.length})</td>\n    </tr>\n  ))\n}","2-depth-chart#2. Depth Chart":"Visual representation of liquidity:\nrenderDepthChart() {\n  const buyDepth = this.calculateDepth(this.props.orderBook.buyOrders)\n  const sellDepth = this.calculateDepth(this.props.orderBook.sellOrders)\n  \n  return (\n    <div className=\"depth-chart\">\n      {/* Render bars for each price level */}\n      {buyDepth.map(level => (\n        <div \n          key={level.price}\n          className=\"depth-bar buy\"\n          style={{ width: `${level.percentage}%` }}\n        />\n      ))}\n    </div>\n  )\n}\ncalculateDepth(orders) {\n  let cumulative = 0\n  const maxVolume = orders.reduce((sum, o) => \n    sum + parseFloat(o.etherAmount), 0\n  )\n  \n  return orders.map(order => {\n    cumulative += parseFloat(order.etherAmount)\n    return {\n      price: order.tokenPrice,\n      volume: cumulative,\n      percentage: (cumulative / maxVolume) * 100\n    }\n  })\n}","3-spread-display#3. Spread Display":"Show bid-ask spread:\nrenderSpread() {\n  const { buyOrders, sellOrders } = this.props.orderBook\n  \n  if (buyOrders.length === 0 || sellOrders.length === 0) {\n    return null\n  }\n  \n  const bestBid = buyOrders[0].tokenPrice\n  const bestAsk = sellOrders[0].tokenPrice\n  const spread = bestAsk - bestBid\n  const spreadPercent = (spread / bestAsk) * 100\n  \n  return (\n    <div className=\"spread-display\">\n      <small className=\"text-muted\">\n        Spread: {spread.toFixed(5)} ETH ({spreadPercent.toFixed(2)}%)\n      </small>\n    </div>\n  )\n}","4-order-size-indicators#4. Order Size Indicators":"Visual size representation:\nrenderOrderWithSize(order, maxSize) {\n  const sizePercent = (order.etherAmount / maxSize) * 100\n  \n  return (\n    <tr key={order.id}>\n      <td>\n        <div className=\"order-size-container\">\n          <div \n            className=\"order-size-bar\"\n            style={{ width: `${sizePercent}%` }}\n          />\n          <span className=\"order-size-text\">\n            {order.tokenAmount}\n          </span>\n        </div>\n      </td>\n      <td>{order.tokenPrice}</td>\n      <td>{order.etherAmount}</td>\n    </tr>\n  )\n}","5-filtering-options#5. Filtering Options":"Filter by minimum size:\nstate = {\n  minOrderSize: 0\n}\nrenderFilterControls() {\n  return (\n    <div className=\"order-filters mb-2\">\n      <label className=\"form-label small\">Min Order Size (ETH)</label>\n      <input\n        type=\"number\"\n        className=\"form-control form-control-sm\"\n        value={this.state.minOrderSize}\n        onChange={(e) => this.setState({ \n          minOrderSize: parseFloat(e.target.value) \n        })}\n        step=\"0.01\"\n        min=\"0\"\n      />\n    </div>\n  )\n}\ngetFilteredOrders(orders) {\n  return orders.filter(order => \n    parseFloat(order.etherAmount) >= this.state.minOrderSize\n  )\n}","6-my-orders-highlighting#6. My Orders Highlighting":"Highlight user's own orders:\nrenderOrder(order, type) {\n  const { account } = this.props\n  const isMyOrder = order.user === account\n  \n  return (\n    <tr\n      className={`\n        order-book-order \n        order-${type}\n        ${isMyOrder ? 'my-order' : ''}\n      `}\n      onClick={!isMyOrder ? (e) => this.handleFillOrder(order) : null}\n    >\n      <td>\n        {isMyOrder && <span className=\"badge bg-primary me-1\">Mine</span>}\n        {order.tokenAmount}\n      </td>\n      <td>{order.tokenPrice}</td>\n      <td>{order.etherAmount}</td>\n    </tr>\n  )\n}\nCSS:\n.my-order {\n  border-left: 3px solid #0d6efd;\n  background-color: rgba(13, 110, 253, 0.05);\n  cursor: default;\n}\n.my-order:hover {\n  background-color: rgba(13, 110, 253, 0.05);\n}","-redux-integration#🔗 Redux Integration":"","selectors#Selectors":"// Get open orders\nconst openOrders = createSelector(\n  allOrders,\n  orders => {\n    return orders.filter(o => o.orderFillAction !== 'filled')\n  }\n)\n// Get order book\nexport const orderBookSelector = createSelector(\n  openOrders,\n  orders => {\n    // Decorate orders\n    orders = decorateOrderBookOrders(orders)\n    \n    // Separate by type\n    const buyOrders = orders.filter(o => o.orderType === 'buy')\n    const sellOrders = orders.filter(o => o.orderType === 'sell')\n    \n    return {\n      buyOrders: buyOrders.sort((a, b) => b.tokenPrice - a.tokenPrice),\n      sellOrders: sellOrders.sort((a, b) => a.tokenPrice - b.tokenPrice)\n    }\n  }\n)\n// Check if order book is loaded\nexport const orderBookLoadedSelector = createSelector(\n  allOrdersLoaded,\n  loaded => loaded\n)","fill-order-action#Fill Order Action":"// store/interactions.js\nexport const fillOrder = (dispatch, exchange, order, account) => {\n  dispatch(orderFilling())\n  \n  exchange.methods.fillOrder(order.id)\n    .send({ from: account })\n    .on('transactionHash', (hash) => {\n      console.log('Transaction hash:', hash)\n    })\n    .on('error', (error) => {\n      console.error(error)\n      window.alert('There was an error filling the order!')\n    })\n}","-common-issues#🐛 Common Issues":"","issue-1-orders-not-displaying#Issue 1: Orders Not Displaying":"Problem: Order book appears emptySolution: Check Redux state\n// Debug selector\nconsole.log('All orders:', this.props.allOrders)\nconsole.log('Open orders:', this.props.openOrders)\nconsole.log('Order book:', this.props.orderBook)\n// Ensure orders are loaded\nif (!this.props.orderBookLoaded) {\n  return <Spinner />\n}","issue-2-fill-order-fails#Issue 2: Fill Order Fails":"Problem: Transaction reverts when filling orderSolution: Validate before filling\nhandleFillOrder = async (order) => {\n  const { account, exchange, token } = this.props\n  \n  // Check if order is still open\n  const orderFilled = await exchange.methods.orderFilled(order.id).call()\n  if (orderFilled) {\n    window.alert('Order already filled!')\n    return\n  }\n  \n  // Check user has enough balance\n  const requiredBalance = order.orderType === 'buy' \n    ? order.tokenAmount \n    : order.etherAmount\n    \n  const balance = await this.getBalance(account)\n  if (balance < requiredBalance) {\n    window.alert('Insufficient balance!')\n    return\n  }\n  \n  // Proceed with fill\n  fillOrder(dispatch, exchange, order, account)\n}","issue-3-incorrect-sorting#Issue 3: Incorrect Sorting":"Problem: Orders not sorted by best priceSolution: Verify sort logic\n// Buy orders: highest price first (descending)\nconst buyOrders = orders\n  .filter(o => o.orderType === 'buy')\n  .sort((a, b) => {\n    return parseFloat(b.tokenPrice) - parseFloat(a.tokenPrice)\n  })\n// Sell orders: lowest price first (ascending)\nconst sellOrders = orders\n  .filter(o => o.orderType === 'sell')\n  .sort((a, b) => {\n    return parseFloat(a.tokenPrice) - parseFloat(b.tokenPrice)\n  })","-testing#🧪 Testing":"import { render, screen, fireEvent } from '@testing-library/react'\nimport { Provider } from 'react-redux'\nimport OrderBook from './OrderBook'\nimport configureStore from '../store/configureStore'\ndescribe('OrderBook Component', () => {\n  let store\n  beforeEach(() => {\n    const mockOrders = [\n      {\n        id: 1,\n        user: '0x123...',\n        tokenAmount: '10',\n        tokenPrice: 0.1,\n        etherAmount: '1',\n        orderType: 'buy'\n      },\n      {\n        id: 2,\n        user: '0x456...',\n        tokenAmount: '20',\n        tokenPrice: 0.11,\n        etherAmount: '2.2',\n        orderType: 'sell'\n      }\n    ]\n    \n    store = configureStore({\n      exchange: {\n        allOrders: { loaded: true, data: mockOrders }\n      }\n    })\n  })\n  it('renders buy and sell orders', () => {\n    render(\n      <Provider store={store}>\n        <OrderBook />\n      </Provider>\n    )\n    \n    expect(screen.getByText('BUY ORDERS')).toBeInTheDocument()\n    expect(screen.getByText('SELL ORDERS')).toBeInTheDocument()\n  })\n  it('calls fillOrder on order click', () => {\n    const { container } = render(\n      <Provider store={store}>\n        <OrderBook />\n      </Provider>\n    )\n    \n    const order = container.querySelector('.order-book-order')\n    fireEvent.click(order)\n    \n    // Assert fillOrder was called\n  })\n  it('displays orders sorted by price', () => {\n    render(\n      <Provider store={store}>\n        <OrderBook />\n      </Provider>\n    )\n    \n    const prices = screen.getAllByText(/0\\.\\d+/)\n    // Assert prices are in correct order\n  })\n})","-performance-optimization#📊 Performance Optimization":"","1-memoization#1. Memoization":"import { memo } from 'react'\nconst OrderRow = memo(({ order, onClick }) => (\n  <tr onClick={onClick}>\n    <td>{order.tokenAmount}</td>\n    <td>{order.tokenPrice}</td>\n    <td>{order.etherAmount}</td>\n  </tr>\n))","2-virtual-scrolling#2. Virtual Scrolling":"import { FixedSizeList } from 'react-window'\nrenderVirtualizedOrders() {\n  const Row = ({ index, style }) => {\n    const order = this.props.orderBook.buyOrders[index]\n    return (\n      <div style={style}>\n        {this.renderOrder(order, 'buy')}\n      </div>\n    )\n  }\n  return (\n    <FixedSizeList\n      height={400}\n      itemCount={this.props.orderBook.buyOrders.length}\n      itemSize={35}\n      width=\"100%\"\n    >\n      {Row}\n    </FixedSizeList>\n  )\n}","3-debounced-updates#3. Debounced Updates":"import { debounce } from 'lodash'\ncomponentDidUpdate(prevProps) {\n  if (prevProps.orderBook !== this.props.orderBook) {\n    this.debouncedUpdate()\n  }\n}\ndebouncedUpdate = debounce(() => {\n  this.forceUpdate()\n}, 300)","-related-components#🔗 Related Components":"NewOrder - Create orders that appear here\nMyTransactions - View filled orders\nPriceChart - Visual price representation\nTrades - Recent executed trades","-related-documentation#📚 Related Documentation":"Exchange Contract - fillOrder function\nState Management - Order book selectors\nAPI Reference - fillOrder interaction\nNext: Learn about the PriceChart Component →"}},"/frontend/tradescomponent":{"title":"Trades Component","data":{"":"Display list of recent filled orders across all users.File: src/components/Trades.js","component-structure#Component Structure":"import React, { Component } from 'react'\nimport { connect } from 'react-redux'\nimport { filledOrdersLoadedSelector, filledOrdersSelector } from '../store/selectors'\nclass Trades extends Component {\n  render() {\n    return (\n      <div className=\"card bg-dark text-white\">\n        <div className=\"card-header\">Trades</div>\n        <div className=\"card-body\">\n          <table className=\"table table-dark table-sm small\">\n            <thead>\n              <tr>\n                <th>Time</th>\n                <th>DAPP</th>\n                <th>DAPP/ETH</th>\n              </tr>\n            </thead>\n            <tbody>\n              {this.props.filledOrders.map(order => (\n                <tr key={order.id} className={`order-${order.tokenPriceClass}`}>\n                  <td className=\"text-muted\">{order.formattedTimestamp}</td>\n                  <td>{order.tokenAmount}</td>\n                  <td className={`text-${order.tokenPriceClass}`}>\n                    {order.tokenPrice}\n                  </td>\n                </tr>\n              ))}\n            </tbody>\n          </table>\n        </div>\n      </div>\n    )\n  }\n}\nfunction mapStateToProps(state) {\n  return {\n    filledOrdersLoaded: filledOrdersLoadedSelector(state),\n    filledOrders: filledOrdersSelector(state)\n  }\n}\nexport default connect(mapStateToProps)(Trades)","key-features#Key Features":"","order-decoration#Order Decoration":"// In selectors\nconst decorateFilledOrders = (orders) => {\n  let previousOrder = orders[0]\n  \n  return orders.map((order) => {\n    order = decorateOrder(order)\n    order = decorateFilledOrder(order, previousOrder)\n    previousOrder = order\n    return order\n  })\n}\nconst decorateFilledOrder = (order, previousOrder) => {\n  return {\n    ...order,\n    tokenPriceClass: tokenPriceClass(order.tokenPrice, order.id, previousOrder)\n  }\n}\nconst tokenPriceClass = (tokenPrice, orderId, previousOrder) => {\n  if (previousOrder.id === orderId) {\n    return GREEN\n  }\n  \n  if (previousOrder.tokenPrice <= tokenPrice) {\n    return GREEN // success\n  } else {\n    return RED // danger\n  }\n}","color-coding#Color Coding":".order-success {\n  background-color: rgba(25, 135, 84, 0.05);\n}\n.order-danger {\n  background-color: rgba(220, 53, 69, 0.05);\n}\n.text-success {\n  color: #25CE8F !important;\n}\n.text-danger {\n  color: #F45353 !important;\n}\nShows user's personal orders (open and filled).File: src/components/MyTransactions.js","component-structure-1#Component Structure":"import React, { Component } from 'react'\nimport { connect } from 'react-redux'\nimport { Tabs, Tab } from 'react-bootstrap'\nimport {\n  myFilledOrdersLoadedSelector,\n  myFilledOrdersSelector,\n  myOpenOrdersLoadedSelector,\n  myOpenOrdersSelector,\n  orderCancellingSelector\n} from '../store/selectors'\nimport { cancelOrder } from '../store/interactions'\nclass MyTransactions extends Component {\n  render() {\n    return (\n      <div className=\"card bg-dark text-white\">\n        <div className=\"card-header\">My Transactions</div>\n        <div className=\"card-body\">\n          <Tabs defaultActiveKey=\"trades\" className=\"bg-dark text-white\">\n            <Tab eventKey=\"trades\" title=\"Trades\">\n              {this.renderFilledOrders()}\n            </Tab>\n            <Tab eventKey=\"orders\" title=\"Orders\">\n              {this.renderOpenOrders()}\n            </Tab>\n          </Tabs>\n        </div>\n      </div>\n    )\n  }\n  renderFilledOrders() {\n    const { myFilledOrders } = this.props\n    \n    return (\n      <table className=\"table table-dark table-sm small\">\n        <thead>\n          <tr>\n            <th>Time</th>\n            <th>DAPP</th>\n            <th>DAPP/ETH</th>\n          </tr>\n        </thead>\n        <tbody>\n          {myFilledOrders.map(order => (\n            <tr key={order.id}>\n              <td className=\"text-muted\">{order.formattedTimestamp}</td>\n              <td className={`text-${order.orderTypeClass}`}>\n                {order.orderSign}{order.tokenAmount}\n              </td>\n              <td className={`text-${order.orderTypeClass}`}>\n                {order.tokenPrice}\n              </td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n    )\n  }\n  renderOpenOrders() {\n    const { myOpenOrders, dispatch, exchange, account } = this.props\n    \n    return (\n      <table className=\"table table-dark table-sm small\">\n        <thead>\n          <tr>\n            <th>Amount</th>\n            <th>DAPP/ETH</th>\n            <th>Cancel</th>\n          </tr>\n        </thead>\n        <tbody>\n          {myOpenOrders.map(order => (\n            <tr key={order.id}>\n              <td className={`text-${order.orderTypeClass}`}>\n                {order.orderSign}{order.tokenAmount}\n              </td>\n              <td className={`text-${order.orderTypeClass}`}>\n                {order.tokenPrice}\n              </td>\n              <td>\n                <button\n                  className=\"btn btn-sm btn-danger\"\n                  onClick={(e) => {\n                    cancelOrder(dispatch, exchange, order, account)\n                  }}\n                >\n                  X\n                </button>\n              </td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n    )\n  }\n}\nfunction mapStateToProps(state) {\n  const myOpenOrdersLoaded = myOpenOrdersLoadedSelector(state)\n  const orderCancelling = orderCancellingSelector(state)\n  return {\n    myFilledOrders: myFilledOrdersSelector(state),\n    myFilledOrdersLoaded: myFilledOrdersLoadedSelector(state),\n    myOpenOrders: myOpenOrdersSelector(state),\n    myOpenOrdersLoaded,\n    showMyOpenOrders: myOpenOrdersLoaded && !orderCancelling,\n    exchange: exchangeSelector(state),\n    account: accountSelector(state)\n  }\n}\nexport default connect(mapStateToProps)(MyTransactions)","key-features-1#Key Features":"","filter-user-orders#Filter User Orders":"// Selectors\nexport const myFilledOrdersSelector = createSelector(\n  accountSelector,\n  filledOrders,\n  (account, orders) => {\n    orders = orders.filter((o) => o.user === account || o.userFill === account)\n    orders = orders.sort((a, b) => b.timestamp - a.timestamp)\n    orders = decorateMyFilledOrders(orders, account)\n    return orders\n  }\n)\nexport const myOpenOrdersSelector = createSelector(\n  accountSelector,\n  openOrders,\n  (account, orders) => {\n    orders = orders.filter((o) => o.user === account)\n    orders = orders.sort((a, b) => b.timestamp - a.timestamp)\n    orders = decorateMyOpenOrders(orders)\n    return orders\n  }\n)","order-sign#Order Sign":"const decorateMyFilledOrders = (orders, account) => {\n  return orders.map((order) => {\n    order = decorateOrder(order)\n    order = decorateMyFilledOrder(order, account)\n    return order\n  })\n}\nconst decorateMyFilledOrder = (order, account) => {\n  const myOrder = order.user === account\n  let orderType\n  if (myOrder) {\n    orderType = order.tokenGive === ETHER_ADDRESS ? 'buy' : 'sell'\n  } else {\n    orderType = order.tokenGive === ETHER_ADDRESS ? 'sell' : 'buy'\n  }\n  return {\n    ...order,\n    orderType,\n    orderTypeClass: (orderType === 'buy' ? 'success' : 'danger'),\n    orderSign: (orderType === 'buy' ? '+' : '-')\n  }\n}","cancel-order#Cancel Order":"// interactions.js\nexport const cancelOrder = (dispatch, exchange, order, account) => {\n  exchange.methods.cancelOrder(order.id)\n    .send({ from: account })\n    .on('transactionHash', (hash) => {\n      dispatch(orderCancelling())\n    })\n    .on('error', (error) => {\n      console.error(error)\n      window.alert('There was an error!')\n    })\n}\nForm for creating new buy/sell orders.File: src/components/NewOrder.js","component-structure-2#Component Structure":"import React, { Component } from 'react'\nimport { connect } from 'react-redux'\nimport { Tabs, Tab } from 'react-bootstrap'\nimport {\n  exchangeSelector,\n  tokenSelector,\n  accountSelector,\n  web3Selector,\n  buyOrderSelector,\n  sellOrderSelector\n} from '../store/selectors'\nimport {\n  buyOrderAmountChanged,\n  buyOrderPriceChanged,\n  sellOrderAmountChanged,\n  sellOrderPriceChanged\n} from '../store/actions'\nimport { makeBuyOrder, makeSellOrder } from '../store/interactions'\nclass NewOrder extends Component {\n  render() {\n    return (\n      <div className=\"card bg-dark text-white\">\n        <div className=\"card-header\">New Order</div>\n        <div className=\"card-body\">\n          <Tabs defaultActiveKey=\"buy\" className=\"bg-dark text-white\">\n            <Tab eventKey=\"buy\" title=\"Buy\" className=\"bg-dark\">\n              {this.renderBuyForm()}\n            </Tab>\n            <Tab eventKey=\"sell\" title=\"Sell\" className=\"bg-dark\">\n              {this.renderSellForm()}\n            </Tab>\n          </Tabs>\n        </div>\n      </div>\n    )\n  }\n  renderBuyForm() {\n    const {\n      dispatch,\n      buyOrder,\n      web3,\n      exchange,\n      token,\n      account,\n      showBuyTotal\n    } = this.props\n    return (\n      <form onSubmit={(event) => {\n        event.preventDefault()\n        makeBuyOrder(dispatch, exchange, token, web3, buyOrder, account)\n      }}>\n        <div className=\"form-group small\">\n          <label>Buy Amount (DAPP)</label>\n          <div className=\"input-group\">\n            <input\n              type=\"text\"\n              className=\"form-control form-control-sm bg-dark text-white\"\n              placeholder=\"Buy Amount\"\n              onChange={(e) => dispatch(buyOrderAmountChanged(e.target.value))}\n              required\n            />\n          </div>\n        </div>\n        <div className=\"form-group small\">\n          <label>Buy Price</label>\n          <div className=\"input-group\">\n            <input\n              type=\"text\"\n              className=\"form-control form-control-sm bg-dark text-white\"\n              placeholder=\"Buy Price\"\n              onChange={(e) => dispatch(buyOrderPriceChanged(e.target.value))}\n              required\n            />\n          </div>\n        </div>\n        <button type=\"submit\" className=\"btn btn-primary btn-sm btn-block\">\n          Buy Order\n        </button>\n        {showBuyTotal && (\n          <small className=\"text-muted\">\n            Total: {buyOrder.amount * buyOrder.price} ETH\n          </small>\n        )}\n      </form>\n    )\n  }\n  renderSellForm() {\n    const {\n      dispatch,\n      sellOrder,\n      web3,\n      exchange,\n      token,\n      account,\n      showSellTotal\n    } = this.props\n    return (\n      <form onSubmit={(event) => {\n        event.preventDefault()\n        makeSellOrder(dispatch, exchange, token, web3, sellOrder, account)\n      }}>\n        <div className=\"form-group small\">\n          <label>Sell Amount (DAPP)</label>\n          <div className=\"input-group\">\n            <input\n              type=\"text\"\n              className=\"form-control form-control-sm bg-dark text-white\"\n              placeholder=\"Sell Amount\"\n              onChange={(e) => dispatch(sellOrderAmountChanged(e.target.value))}\n              required\n            />\n          </div>\n        </div>\n        <div className=\"form-group small\">\n          <label>Sell Price</label>\n          <div className=\"input-group\">\n            <input\n              type=\"text\"\n              className=\"form-control form-control-sm bg-dark text-white\"\n              placeholder=\"Sell Price\"\n              onChange={(e) => dispatch(sellOrderPriceChanged(e.target.value))}\n              required\n            />\n          </div>\n        </div>\n        <button type=\"submit\" className=\"btn btn-primary btn-sm btn-block\">\n          Sell Order\n        </button>\n        {showSellTotal && (\n          <small className=\"text-muted\">\n            Total: {sellOrder.amount * sellOrder.price} ETH\n          </small>\n        )}\n      </form>\n    )\n  }\n}\nfunction mapStateToProps(state) {\n  const buyOrder = buyOrderSelector(state)\n  const sellOrder = sellOrderSelector(state)\n  return {\n    account: accountSelector(state),\n    exchange: exchangeSelector(state),\n    token: tokenSelector(state),\n    web3: web3Selector(state),\n    buyOrder,\n    sellOrder,\n    showBuyTotal: buyOrder.amount && buyOrder.price,\n    showSellTotal: sellOrder.amount && sellOrder.price\n  }\n}\nexport default connect(mapStateToProps)(NewOrder)","key-features-2#Key Features":"","order-creation#Order Creation":"// interactions.js\nexport const makeBuyOrder = (dispatch, exchange, token, web3, order, account) => {\n  const tokenGet = token.options.address\n  const amountGet = web3.utils.toWei(order.amount, 'ether')\n  const tokenGive = ETHER_ADDRESS\n  const amountGive = web3.utils.toWei((order.amount * order.price).toString(), 'ether')\n  exchange.methods.makeOrder(tokenGet, amountGet, tokenGive, amountGive)\n    .send({ from: account })\n    .on('transactionHash', (hash) => {\n      dispatch(buyOrderMaking())\n    })\n    .on('error', (error) => {\n      console.error(error)\n      window.alert('There was an error!')\n    })\n}\nexport const makeSellOrder = (dispatch, exchange, token, web3, order, account) => {\n  const tokenGet = ETHER_ADDRESS\n  const amountGet = web3.utils.toWei((order.amount * order.price).toString(), 'ether')\n  const tokenGive = token.options.address\n  const amountGive = web3.utils.toWei(order.amount, 'ether')\n  exchange.methods.makeOrder(tokenGet, amountGet, tokenGive, amountGive)\n    .send({ from: account })\n    .on('transactionHash', (hash) => {\n      dispatch(sellOrderMaking())\n    })\n    .on('error', (error) => {\n      console.error(error)\n      window.alert('There was an error!')\n    })\n}","form-validation#Form Validation":"validateOrder(amount, price, balance) {\n  if (!amount || !price) {\n    return { valid: false, message: 'Please fill all fields' }\n  }\n  \n  if (parseFloat(amount) <= 0) {\n    return { valid: false, message: 'Amount must be greater than 0' }\n  }\n  \n  if (parseFloat(price) <= 0) {\n    return { valid: false, message: 'Price must be greater than 0' }\n  }\n  \n  const total = parseFloat(amount) * parseFloat(price)\n  if (total > parseFloat(balance)) {\n    return { valid: false, message: 'Insufficient balance' }\n  }\n  \n  return { valid: true }\n}\nhandleSubmit = (event) => {\n  event.preventDefault()\n  \n  const validation = this.validateOrder(\n    this.props.buyOrder.amount,\n    this.props.buyOrder.price,\n    this.props.ethBalance\n  )\n  \n  if (!validation.valid) {\n    window.alert(validation.message)\n    return\n  }\n  \n  // Submit order\n  makeBuyOrder(...)\n}","quick-price-selection#Quick Price Selection":"renderPricePresets() {\n  const presets = [0.05, 0.10, 0.15, 0.20]\n  \n  return (\n    <div className=\"price-presets\">\n      {presets.map(price => (\n        <button\n          key={price}\n          type=\"button\"\n          className=\"btn btn-sm btn-outline-secondary me-1\"\n          onClick={() => this.props.dispatch(buyOrderPriceChanged(price.toString()))}\n        >\n          {price}\n        </button>\n      ))}\n    </div>\n  )\n}","amount-percentage-buttons#Amount Percentage Buttons":"renderAmountPercentages() {\n  const percentages = [25, 50, 75, 100]\n  \n  return (\n    <div className=\"amount-percentages\">\n      {percentages.map(pct => (\n        <button\n          key={pct}\n          type=\"button\"\n          className=\"btn btn-sm btn-outline-secondary\"\n          onClick={() => this.setAmountByPercentage(pct)}\n        >\n          {pct}%\n        </button>\n      ))}\n    </div>\n  )\n}\nsetAmountByPercentage(percentage) {\n  const { tokenBalance } = this.props\n  const amount = (tokenBalance * percentage) / 100\n  this.props.dispatch(sellOrderAmountChanged(amount.toString()))\n}","redux-actions#Redux Actions":"// actions.js\nexport function buyOrderAmountChanged(amount) {\n  return {\n    type: 'BUY_ORDER_AMOUNT_CHANGED',\n    amount\n  }\n}\nexport function buyOrderPriceChanged(price) {\n  return {\n    type: 'BUY_ORDER_PRICE_CHANGED',\n    price\n  }\n}\n// Similar for sell orders...","redux-selectors#Redux Selectors":"// selectors.js\nexport const buyOrderSelector = createSelector(\n  state => state.exchange.buyOrder,\n  order => order\n)\nexport const sellOrderSelector = createSelector(\n  state => state.exchange.sellOrder,\n  order => order\n)"}},"/frontend/pricechart":{"title":"PriceChart Component","data":{"":"Interactive candlestick chart displaying token price movements using ApexCharts.","-overview#📋 Overview":"File: src/components/PriceChart.jsPurpose:\nVisualize price movements over time\nDisplay OHLC (Open, High, Low, Close) data\nShow trading volume\nProvide interactive chart controls\nReal-time price updates","-visual-structure#🎨 Visual Structure":"┌────────────────────────────────────────┐\n│          Price Chart                   │\n├────────────────────────────────────────┤\n│  Price: 0.105 ETH  (+5.2%)            │\n├────────────────────────────────────────┤\n│                   ▲                    │\n│    ┃             ┃│                    │\n│  ┃ ┃   ┃      ┃ ┃│┃                   │\n│  ┃ ┃┃  ┃┃  ┃  ┃ ┃│┃                   │\n│  ┃ ┃┃  ┃┃  ┃┃ ┃ ┃│┃  ┃                │\n│ ━┃━┃┃━━┃┃━━┃┃━┃━┃│┃━━┃━▶              │\n│  10:00  10:30  11:00  11:30           │\n└────────────────────────────────────────┘","️-component-structure#🏗️ Component Structure":"import React, { Component } from 'react'\nimport { connect } from 'react-redux'\nimport Chart from 'react-apexcharts'\nimport Spinner from './Spinner'\nimport {\n  priceChartLoadedSelector,\n  priceChartSelector\n} from '../store/selectors'\nconst priceSymbol = (lastPriceChange) => {\n  let output\n  if(lastPriceChange === '+') {\n    output = <span className=\"text-success\">&#9650;</span> // Green up\n  } else {\n    output = <span className=\"text-danger\">&#9660;</span> // Red down\n  }\n  return(output)\n}\nclass PriceChart extends Component {\n  render() {\n    return (\n      <div className=\"card bg-dark text-white\">\n        <div className=\"card-header\">Price Chart</div>\n        <div className=\"card-body\">\n          {this.props.priceChartLoaded ? (\n            this.renderChart()\n          ) : (\n            <Spinner type=\"div\" />\n          )}\n        </div>\n      </div>\n    )\n  }\n  renderChart() {\n    return (\n      <div className=\"price-chart\">\n        <div className=\"price-stats\">\n          <span className=\"price-label\">DAPP/ETH {priceSymbol(this.props.priceChart.lastPriceChange)}</span>\n          <span className=\"price-value\">{this.props.priceChart.lastPrice}</span>\n        </div>\n        <Chart\n          options={this.props.priceChart.options}\n          series={this.props.priceChart.series}\n          type='candlestick'\n          width='100%'\n          height='100%'\n        />\n      </div>\n    )\n  }\n}\nfunction mapStateToProps(state) {\n  return {\n    priceChartLoaded: priceChartLoadedSelector(state),\n    priceChart: priceChartSelector(state)\n  }\n}\nexport default connect(mapStateToProps)(PriceChart)","-chart-configuration#📊 Chart Configuration":"","apexcharts-options#ApexCharts Options":"const chartOptions = {\n  chart: {\n    animations: { enabled: true },\n    toolbar: { show: false },\n    width: '100%'\n  },\n  tooltip: {\n    enabled: true,\n    theme: 'dark',\n    custom: function({ seriesIndex, dataPointIndex, w }) {\n      const o = w.globals.seriesCandleO[seriesIndex][dataPointIndex]\n      const h = w.globals.seriesCandleH[seriesIndex][dataPointIndex]\n      const l = w.globals.seriesCandleL[seriesIndex][dataPointIndex]\n      const c = w.globals.seriesCandleC[seriesIndex][dataPointIndex]\n      return (\n        '<div class=\"apexcharts-tooltip-candlestick\">' +\n          '<div>Open: <span>' + o.toFixed(5) + '</span></div>' +\n          '<div>High: <span>' + h.toFixed(5) + '</span></div>' +\n          '<div>Low: <span>' + l.toFixed(5) + '</span></div>' +\n          '<div>Close: <span>' + c.toFixed(5) + '</span></div>' +\n        '</div>'\n      )\n    }\n  },\n  grid: {\n    show: true,\n    borderColor: '#767F92',\n    strokeDashArray: 0\n  },\n  plotOptions: {\n    candlestick: {\n      colors: {\n        upward: '#25CE8F',\n        downward: '#F45353'\n      }\n    }\n  },\n  xaxis: {\n    type: 'datetime',\n    labels: {\n      style: {\n        colors: '#767F92',\n        fontSize: '14px',\n        cssClass: 'apexcharts-xaxis-label',\n      },\n    }\n  },\n  yaxis: {\n    labels: {\n      style: {\n        colors: '#767F92',\n        fontSize: '14px',\n        cssClass: 'apexcharts-yaxis-label',\n      },\n      formatter: function(val) {\n        return val.toFixed(5)\n      }\n    }\n  }\n}","chart-series-data#Chart Series Data":"const series = [\n  {\n    name: 'Price',\n    data: [\n      {\n        x: new Date(1538778600000),\n        y: [0.0995, 0.1020, 0.0990, 0.1015] // [open, high, low, close]\n      },\n      {\n        x: new Date(1538780400000),\n        y: [0.1015, 0.1050, 0.1010, 0.1045]\n      },\n      // More data points...\n    ]\n  }\n]","-data-processing#🔄 Data Processing":"","building-chart-data#Building Chart Data":"// store/selectors.js\nexport const priceChartSelector = createSelector(\n  filledOrders,\n  orders => {\n    // Sort by timestamp\n    orders = orders.sort((a, b) => a.timestamp - b.timestamp)\n    \n    // Format data for chart\n    orders = orders.map(o => decorateOrder(o))\n    \n    // Group by hour\n    let groupedOrders = groupBy(orders, (o) => \n      moment.unix(o.timestamp).startOf('hour').format()\n    )\n    \n    // Build candlestick data\n    const series = Object.keys(groupedOrders).map(hour => {\n      const group = groupedOrders[hour]\n      \n      return {\n        x: new Date(hour),\n        y: [\n          group[0].tokenPrice,                    // Open\n          Math.max(...group.map(o => o.tokenPrice)), // High\n          Math.min(...group.map(o => o.tokenPrice)), // Low\n          group[group.length - 1].tokenPrice      // Close\n        ]\n      }\n    })\n    \n    // Calculate last price change\n    const lastPriceChange = series.length >= 2\n      ? series[series.length - 1].y[3] >= series[series.length - 2].y[3] ? '+' : '-'\n      : '+'\n    \n    return {\n      series: [{ name: 'Price', data: series }],\n      options: chartOptions,\n      lastPrice: series.length > 0 \n        ? series[series.length - 1].y[3].toFixed(5) \n        : '0.00000',\n      lastPriceChange\n    }\n  }\n)","time-grouping#Time Grouping":"Group by different intervals:\ngroupOrdersByTime(orders, interval = 'hour') {\n  return groupBy(orders, (order) => {\n    switch(interval) {\n      case 'minute':\n        return moment.unix(order.timestamp).startOf('minute').format()\n      case 'hour':\n        return moment.unix(order.timestamp).startOf('hour').format()\n      case 'day':\n        return moment.unix(order.timestamp).startOf('day').format()\n      default:\n        return moment.unix(order.timestamp).startOf('hour').format()\n    }\n  })\n}","ohlc-calculation#OHLC Calculation":"calculateOHLC(trades) {\n  if (trades.length === 0) {\n    return { open: 0, high: 0, low: 0, close: 0 }\n  }\n  \n  const prices = trades.map(t => parseFloat(t.tokenPrice))\n  \n  return {\n    open: prices[0],\n    high: Math.max(...prices),\n    low: Math.min(...prices),\n    close: prices[prices.length - 1]\n  }\n}","-styling#🎨 Styling":"","chart-container#Chart Container":".price-chart {\n  height: 100%;\n  min-height: 300px;\n}\n.price-stats {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 10px 0;\n  border-bottom: 1px solid #2c3e50;\n  margin-bottom: 10px;\n}\n.price-label {\n  font-size: 0.875rem;\n  font-weight: 600;\n  text-transform: uppercase;\n}\n.price-value {\n  font-size: 1.25rem;\n  font-weight: 700;\n  font-family: 'Courier New', monospace;\n}","custom-tooltip-styling#Custom Tooltip Styling":".apexcharts-tooltip-candlestick {\n  background: #1a1d24;\n  border: 1px solid #2c3e50;\n  padding: 10px;\n  border-radius: 4px;\n}\n.apexcharts-tooltip-candlestick div {\n  display: flex;\n  justify-content: space-between;\n  padding: 3px 0;\n  color: #fff;\n}\n.apexcharts-tooltip-candlestick span {\n  font-weight: 600;\n  margin-left: 10px;\n}","color-themes#Color Themes":"const chartTheme = {\n  light: {\n    upward: '#26a69a',\n    downward: '#ef5350',\n    grid: '#e0e0e0',\n    text: '#424242'\n  },\n  dark: {\n    upward: '#25CE8F',\n    downward: '#F45353',\n    grid: '#767F92',\n    text: '#767F92'\n  }\n}","-advanced-features#🎯 Advanced Features":"","1-time-range-selector#1. Time Range Selector":"state = {\n  timeRange: '1h'\n}\nrenderTimeSelector() {\n  const ranges = ['5m', '15m', '1h', '4h', '1d', '1w']\n  \n  return (\n    <div className=\"time-selector btn-group btn-group-sm\">\n      {ranges.map(range => (\n        <button\n          key={range}\n          className={`btn btn-outline-secondary ${\n            this.state.timeRange === range ? 'active' : ''\n          }`}\n          onClick={() => this.setState({ timeRange: range })}\n        >\n          {range}\n        </button>\n      ))}\n    </div>\n  )\n}\ngetFilteredData() {\n  const { timeRange } = this.state\n  const now = moment()\n  let cutoff\n  \n  switch(timeRange) {\n    case '5m':\n      cutoff = now.subtract(5, 'minutes')\n      break\n    case '15m':\n      cutoff = now.subtract(15, 'minutes')\n      break\n    case '1h':\n      cutoff = now.subtract(1, 'hour')\n      break\n    case '4h':\n      cutoff = now.subtract(4, 'hours')\n      break\n    case '1d':\n      cutoff = now.subtract(1, 'day')\n      break\n    case '1w':\n      cutoff = now.subtract(1, 'week')\n      break\n    default:\n      cutoff = now.subtract(1, 'hour')\n  }\n  \n  return this.props.priceChart.series[0].data.filter(point =>\n    moment(point.x).isAfter(cutoff)\n  )\n}","2-chart-type-toggle#2. Chart Type Toggle":"state = {\n  chartType: 'candlestick'\n}\nrenderChartTypeToggle() {\n  return (\n    <div className=\"chart-type-toggle\">\n      <button\n        className={this.state.chartType === 'candlestick' ? 'active' : ''}\n        onClick={() => this.setState({ chartType: 'candlestick' })}\n      >\n        Candlestick\n      </button>\n      <button\n        className={this.state.chartType === 'line' ? 'active' : ''}\n        onClick={() => this.setState({ chartType: 'line' })}\n      >\n        Line\n      </button>\n      <button\n        className={this.state.chartType === 'area' ? 'active' : ''}\n        onClick={() => this.setState({ chartType: 'area' })}\n      >\n        Area\n      </button>\n    </div>\n  )\n}\nrender() {\n  return (\n    <Chart\n      options={this.props.priceChart.options}\n      series={this.getSeriesForChartType()}\n      type={this.state.chartType}\n      width='100%'\n      height='100%'\n    />\n  )\n}\ngetSeriesForChartType() {\n  if (this.state.chartType === 'candlestick') {\n    return this.props.priceChart.series\n  }\n  \n  // Convert candlestick data to line/area\n  return [{\n    name: 'Price',\n    data: this.props.priceChart.series[0].data.map(point => ({\n      x: point.x,\n      y: point.y[3] // Use close price\n    }))\n  }]\n}","3-volume-indicator#3. Volume Indicator":"renderWithVolume() {\n  const volumeSeries = [{\n    name: 'Volume',\n    type: 'column',\n    data: this.calculateVolume()\n  }]\n  \n  const combinedOptions = {\n    ...chartOptions,\n    chart: {\n      ...chartOptions.chart,\n      height: 450,\n      type: 'candlestick'\n    },\n    plotOptions: {\n      ...chartOptions.plotOptions,\n      bar: {\n        columnWidth: '80%',\n        colors: {\n          ranges: [{\n            from: 0,\n            to: 999999,\n            color: '#26a69a'\n          }]\n        }\n      }\n    },\n    yaxis: [\n      {\n        // Price axis\n        labels: {\n          formatter: (val) => val.toFixed(5)\n        }\n      },\n      {\n        // Volume axis\n        opposite: true,\n        labels: {\n          formatter: (val) => val.toFixed(2)\n        }\n      }\n    ]\n  }\n  \n  return (\n    <Chart\n      options={combinedOptions}\n      series={[...this.props.priceChart.series, ...volumeSeries]}\n      type=\"candlestick\"\n      height={450}\n    />\n  )\n}\ncalculateVolume() {\n  // Group trades and sum volumes\n  const groupedTrades = groupBy(\n    this.props.filledOrders,\n    o => moment.unix(o.timestamp).startOf('hour').format()\n  )\n  \n  return Object.keys(groupedTrades).map(time => ({\n    x: new Date(time),\n    y: groupedTrades[time].reduce((sum, trade) => \n      sum + parseFloat(trade.etherAmount), 0\n    )\n  }))\n}","4-technical-indicators#4. Technical Indicators":"Moving Average:\ncalculateMovingAverage(data, period) {\n  const ma = []\n  \n  for (let i = period - 1; i < data.length; i++) {\n    const slice = data.slice(i - period + 1, i + 1)\n    const sum = slice.reduce((acc, point) => \n      acc + point.y[3], 0 // Use close price\n    )\n    ma.push({\n      x: data[i].x,\n      y: sum / period\n    })\n  }\n  \n  return ma\n}\nrenderWithMA() {\n  const ma20 = this.calculateMovingAverage(\n    this.props.priceChart.series[0].data, \n    20\n  )\n  \n  const series = [\n    ...this.props.priceChart.series,\n    {\n      name: 'MA(20)',\n      type: 'line',\n      data: ma20\n    }\n  ]\n  \n  return <Chart options={chartOptions} series={series} />\n}\nRSI (Relative Strength Index):\ncalculateRSI(data, period = 14) {\n  const changes = []\n  \n  for (let i = 1; i < data.length; i++) {\n    changes.push(data[i].y[3] - data[i-1].y[3])\n  }\n  \n  const gains = changes.map(c => c > 0 ? c : 0)\n  const losses = changes.map(c => c < 0 ? Math.abs(c) : 0)\n  \n  const avgGain = gains.slice(0, period).reduce((a, b) => a + b) / period\n  const avgLoss = losses.slice(0, period).reduce((a, b) => a + b) / period\n  \n  const rs = avgGain / avgLoss\n  const rsi = 100 - (100 / (1 + rs))\n  \n  return rsi\n}\nrenderRSI() {\n  const rsi = this.calculateRSI(this.props.priceChart.series[0].data)\n  \n  return (\n    <div className=\"rsi-indicator\">\n      <span className=\"label\">RSI(14):</span>\n      <span className={`value ${\n        rsi > 70 ? 'text-danger' : \n        rsi < 30 ? 'text-success' : \n        'text-muted'\n      }`}>\n        {rsi.toFixed(2)}\n      </span>\n    </div>\n  )\n}","5-price-alerts#5. Price Alerts":"state = {\n  priceAlerts: []\n}\naddPriceAlert(price, direction) {\n  this.setState(prev => ({\n    priceAlerts: [\n      ...prev.priceAlerts,\n      {\n        id: Date.now(),\n        price: parseFloat(price),\n        direction, // 'above' or 'below'\n        triggered: false\n      }\n    ]\n  }))\n}\ncomponentDidUpdate(prevProps) {\n  if (prevProps.priceChart.lastPrice !== this.props.priceChart.lastPrice) {\n    this.checkPriceAlerts()\n  }\n}\ncheckPriceAlerts() {\n  const currentPrice = parseFloat(this.props.priceChart.lastPrice)\n  \n  this.state.priceAlerts.forEach(alert => {\n    if (!alert.triggered) {\n      const shouldTrigger = \n        (alert.direction === 'above' && currentPrice >= alert.price) ||\n        (alert.direction === 'below' && currentPrice <= alert.price)\n      \n      if (shouldTrigger) {\n        this.triggerAlert(alert)\n      }\n    }\n  })\n}\ntriggerAlert(alert) {\n  // Show notification\n  new Notification('Price Alert', {\n    body: `Price ${alert.direction} ${alert.price}`,\n    icon: '/logo.png'\n  })\n  \n  // Mark as triggered\n  this.setState(prev => ({\n    priceAlerts: prev.priceAlerts.map(a =>\n      a.id === alert.id ? { ...a, triggered: true } : a\n    )\n  }))\n}","6-export-chart#6. Export Chart":"exportChart(format = 'png') {\n  const chart = this.chartRef.current.chart\n  \n  switch(format) {\n    case 'png':\n      chart.dataURI().then(({ imgURI }) => {\n        const link = document.createElement('a')\n        link.href = imgURI\n        link.download = `price-chart-${Date.now()}.png`\n        link.click()\n      })\n      break\n      \n    case 'svg':\n      chart.dataURI({ scale: 1 }).then(({ imgURI }) => {\n        // Download SVG\n      })\n      break\n      \n    case 'csv':\n      this.exportToCSV()\n      break\n  }\n}\nexportToCSV() {\n  const data = this.props.priceChart.series[0].data\n  const csv = [\n    ['Timestamp', 'Open', 'High', 'Low', 'Close'],\n    ...data.map(point => [\n      new Date(point.x).toISOString(),\n      point.y[0],\n      point.y[1],\n      point.y[2],\n      point.y[3]\n    ])\n  ].map(row => row.join(',')).join('\\n')\n  \n  const blob = new Blob([csv], { type: 'text/csv' })\n  const url = URL.createObjectURL(blob)\n  const link = document.createElement('a')\n  link.href = url\n  link.download = `price-data-${Date.now()}.csv`\n  link.click()\n}","-real-time-updates#🔄 Real-time Updates":"","websocket-integration#WebSocket Integration":"componentDidMount() {\n  this.subscribeToTrades()\n}\nsubscribeToTrades() {\n  const { exchange } = this.props\n  \n  this.tradeSubscription = exchange.events.Trade({})\n    .on('data', (event) => {\n      this.handleNewTrade(event)\n    })\n}\nhandleNewTrade(event) {\n  // Update chart with new trade\n  const trade = {\n    timestamp: event.returnValues.timestamp,\n    price: this.calculatePrice(event.returnValues)\n  }\n  \n  this.updateChart(trade)\n}\ncomponentWillUnmount() {\n  if (this.tradeSubscription) {\n    this.tradeSubscription.unsubscribe()\n  }\n}","animation-on-update#Animation on Update":"const chartOptions = {\n  chart: {\n    animations: {\n      enabled: true,\n      easing: 'easeinout',\n      speed: 800,\n      animateGradually: {\n        enabled: true,\n        delay: 150\n      },\n      dynamicAnimation: {\n        enabled: true,\n        speed: 350\n      }\n    }\n  }\n}","-testing#🧪 Testing":"import { render, screen } from '@testing-library/react'\nimport { Provider } from 'react-redux'\nimport PriceChart from './PriceChart'\nimport configureStore from '../store/configureStore'\ndescribe('PriceChart Component', () => {\n  let store\n  beforeEach(() => {\n    const mockData = {\n      series: [{\n        data: [\n          { x: new Date(), y: [0.1, 0.11, 0.09, 0.105] }\n        ]\n      }],\n      lastPrice: '0.10500',\n      lastPriceChange: '+'\n    }\n    \n    store = configureStore({\n      priceChart: mockData\n    })\n  })\n  it('renders price chart', () => {\n    render(\n      <Provider store={store}>\n        <PriceChart />\n      </Provider>\n    )\n    \n    expect(screen.getByText(/Price Chart/i)).toBeInTheDocument()\n  })\n  it('displays current price', () => {\n    render(\n      <Provider store={store}>\n        <PriceChart />\n      </Provider>\n    )\n    \n    expect(screen.getByText(/0\\.10500/)).toBeInTheDocument()\n  })\n  it('shows price change indicator', () => {\n    render(\n      <Provider store={store}>\n        <PriceChart />\n      </Provider>\n    )\n    \n    // Check for up arrow (green triangle)\n    expect(screen.getByText('▲')).toBeInTheDocument()\n  })\n})","-common-issues#🐛 Common Issues":"","issue-1-chart-not-rendering#Issue 1: Chart Not Rendering":"Problem: Chart container is emptySolution: Check data format\n// Ensure data is in correct format\nconsole.log('Chart data:', this.props.priceChart.series)\n// Verify ApexCharts is loaded\nif (typeof Chart === 'undefined') {\n  console.error('ApexCharts not loaded')\n}","issue-2-tooltip-not-showing#Issue 2: Tooltip Not Showing":"Problem: Custom tooltip doesn't displaySolution: Fix tooltip formatter\ntooltip: {\n  enabled: true,\n  custom: function({ seriesIndex, dataPointIndex, w }) {\n    // Ensure data exists\n    if (!w.globals.seriesCandleO) return ''\n    \n    // Return valid HTML\n    return '<div>...</div>'\n  }\n}","issue-3-performance-with-large-datasets#Issue 3: Performance with Large Datasets":"Problem: Chart lags with many data pointsSolution: Implement data decimation\ndecimateData(data, maxPoints = 100) {\n  if (data.length <= maxPoints) return data\n  \n  const step = Math.ceil(data.length / maxPoints)\n  return data.filter((_, index) => index % step === 0)\n}","-related-components#🔗 Related Components":"OrderBook - Order prices feed chart\nTrades - Trade data source\nMyTransactions - User's trade history","-related-documentation#📚 Related Documentation":"ApexCharts Documentation\nState Management - Price chart selectors\nStyling Guide - Chart theming\nNext: Learn about the Trades Component →"}}}